version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "8080"
    networks:
      - skypath

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - skypath

networks:
  skypath:
    driver: bridge
